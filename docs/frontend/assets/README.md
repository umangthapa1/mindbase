# Frontend Assets

This directory contains static assets used by the frontend, such as images, icons, and other media files.

## Usage

Reference assets in HTML or CSS using relative paths, e.g., `assets/image.png`.

## Note

This directory is currently empty. Add assets as needed.
